# 백준 1009 - 분산처리 (Bronze 2)

## 핵심 개념
- pow(a, b) : a^b
- pow(a, b, m) : (a^b) % m
- pow(a, b, m)은 **중간 계산값을 계속 mod로 줄임**

## 시간복잡도
- (a ** b) % m → O(b)
- pow(a, b, m) → O(log b)

## 비유
- ❌ 벽돌 10000개를 한 번에 이동
- ✅ 벽돌 10개씩 나눠서 계속 이동

## 배운 점
- Python 내장 함수 `pow(a, b, m)`은 코딩테스트에서 적극 활용
