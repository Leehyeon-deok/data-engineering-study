Java 인터페이스(Interface) 완전 정리
1. 인터페이스란?

인터페이스(interface) 는
👉 클래스가 반드시 구현해야 할 메서드의 규약(약속) 을 정의한 것이다.

무엇을 할 수 있는지만 정의

어떻게 하는지는 정의하지 않음

구현은 인터페이스를 구현(implements)한 클래스의 책임

📌 한 줄 정의

인터페이스 = 다형성을 위한 설계도 + 구현 강제 규칙

2. 인터페이스의 목적

다중 상속 효과

결합도 감소

표준화된 규약 제공

다형성 극대화

3. 인터페이스 기본 문법
public interface Flyable {
    void fly();
}

구현 클래스
public class Bird implements Flyable {
    @Override
    public void fly() {
        System.out.println("새가 난다");
    }
}


📌 implements 키워드 사용

4. 인터페이스의 특징 (중요 🔥)
4.1 다중 구현 가능
class Duck implements Flyable, Swimmable {
    ...
}


👉 클래스는 다중 상속 ❌
👉 인터페이스는 다중 구현 ⭕

4.2 모든 메서드는 기본적으로 public abstract
void move();


컴파일러가 자동으로 아래처럼 처리

public abstract void move();

4.3 필드는 무조건 상수
int MAX_SPEED = 100;


자동으로

public static final int MAX_SPEED = 100;


📌 인터페이스에는 인스턴스 변수 없음

4.4 생성자 없음

객체 생성 불가

구현 클래스 통해서만 객체 생성

5. 인터페이스 vs 추상 클래스 (시험 단골)
구분	인터페이스	추상 클래스
다중 상속	⭕	❌
필드	상수만 가능	일반 변수 가능
메서드	추상 메서드(기본)	추상+일반
생성자	❌	⭕
목적	규약	확장

📌 “할 수 있다” → 인터페이스
📌 “이다(is-a)” → 추상 클래스

6. default 메서드 (Java 8+)

인터페이스에도 구현된 메서드를 가질 수 있다.

public interface Printer {
    default void print() {
        System.out.println("출력합니다");
    }
}

왜 생겼나?

기존 인터페이스에 메서드 추가 시
👉 구현 클래스 전부 수정해야 하는 문제 해결

7. static 메서드
public interface Calculator {
    static int add(int a, int b) {
        return a + b;
    }
}


인터페이스명으로만 호출

Calculator.add(3, 5);

8. 인터페이스 타입 다형성 (중요 ⭐)
Flyable f = new Bird();
f.fly();


📌 실제 객체는 Bird
📌 참조 타입은 Flyable

👉 결합도 감소 + 유연한 설계

9. 인터페이스와 의존성 주입(DI)
class Zoo {
    Flyable animal;

    Zoo(Flyable animal) {
        this.animal = animal;
    }
}


구현체 교체 쉬움

테스트, 유지보수에 유리

10. 인터페이스 사용 시 주의점

필드에 상태 저장 ❌

구현 강제성이 강함

너무 많은 메서드는 설계 악몽

11. 정처기 실기·객관식 포인트 🚨

인터페이스의 필드는 public static final

메서드는 public abstract

다중 구현 가능

생성자 없음

implements 키워드 사용

인터페이스 타입으로 객체 참조 가능


✍️ 한 줄 암기

인터페이스는 “할 수 있다”를 표현하는 규약이며
다중 구현과 다형성을 위한 핵심 도구이다.