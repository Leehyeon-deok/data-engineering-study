# JavaScript 옵저버 패턴과 프록시(Proxy) 정리

> 이 문서는 **JavaScript의 Proxy 객체**, **옵저버 패턴**, **프록시 패턴**, 그리고 **프록시 서버/Cloudflare**까지
> 프런트엔드와 웹 인프라에서의 프록시 개념을 한 흐름으로 간략히 정리한 자료이다.

---

## 1. JavaScript 옵저버 패턴 (Observer Pattern)

옵저버 패턴은

> **어떤 대상(Subject)의 상태가 변경되면, 이를 관찰(Observer)하고 있는 객체들에게 자동으로 알리는 패턴**이다.

### 특징

* 상태 변경 → 자동 반응
* 데이터와 화면(DOM)의 동기화에 적합
* 프런트엔드 프레임워크의 핵심 개념

---

## 2. JavaScript Proxy 객체

Proxy 객체는

> **객체의 기본 동작을 가로채(intercept) 원하는 로직을 삽입할 수 있는 객체**이다.

### 가로챌 수 있는 동작 예시

* 속성 접근 (`get`)
* 속성 할당 (`set`)
* 삭제 (`deleteProperty`)
* 순회 / 열거 (`ownKeys`)
* 함수 호출 (`apply`)

---

## 3. Proxy 객체의 구성

JavaScript의 Proxy는 **두 개의 매개변수**를 가진다.

### 1️⃣ target

* 프록시로 감쌀 대상 객체

### 2️⃣ handler

* target의 동작을 가로채서 수행할 로직이 정의된 객체

```js
const proxy = new Proxy(target, handler)
```

---

## 4. Proxy를 이용한 옵저버 패턴

Proxy를 사용하면 **객체의 값 변경을 감지**할 수 있다.

* `set` 트랩을 통해 값 변경 감지
* 값 변경 시 콜백 실행

👉 이 방식으로 **옵저버 패턴 구현 가능**

---

## 5. Vue.js 3.0의 옵저버 패턴

Vue.js 3.0에서는

* `ref`
* `reactive`

로 선언된 데이터가 변경되면 **자동으로 DOM이 갱신**된다.

### 동작 원리

* Vue 내부에서 **Proxy 객체 사용**
* 데이터 접근 / 변경을 가로챔
* 변경 발생 시 DOM 업데이트 트리거

📌 즉,

> **Vue 3의 반응성 시스템은 Proxy 기반 옵저버 패턴 구현체**이다.

---

## 6. 프록시 패턴 (Proxy Pattern)

프록시 패턴은 디자인 패턴 중 하나로,

> **대상 객체에 직접 접근하지 않고, 그 앞단에서 접근을 제어하는 인터페이스를 두는 패턴**이다.

### 목적

* 접근 제어
* 기능 보완
* 부가 로직 삽입

### 활용 예

* 보안
* 데이터 검증
* 캐싱
* 로깅

---

## 7. 프록시 서버에서의 캐싱

프록시 서버는 **요청과 응답을 중간에서 가로채는 서버**이다.

### 캐싱 동작

* 요청 결과를 캐시에 저장
* 동일 요청 발생 시

  * 원격 서버에 다시 요청 ❌
  * 캐시 데이터 사용 ⭕

### 장점

* 네트워크 트래픽 감소
* 응답 속도 향상
* 서버 부하 감소

---

## 8. 프록시 서버(Proxy Server)

프록시 서버란,

> **클라이언트와 서버 사이에서 중계 역할을 수행하는 시스템**이다.

### 역할

* 간접 접속 제공
* 트래픽 제어
* 보안 강화
* 캐싱

---

## 9. Cloudflare와 프록시 서버

Cloudflare는

> **전 세계에 분산된 서버를 기반으로 한 CDN(Content Delivery Network) 서비스**이다.

### Cloudflare의 위치

```text
사용자 → Cloudflare(프록시) → 실제 웹 서버
```

---

## 10. Cloudflare의 주요 기능

### 1️⃣ CDN

* 사용자와 가장 가까운 서버에서 콘텐츠 제공
* 이미지, 영상, 정적 파일 전송 가속

### 2️⃣ 보안 (프록시 역할)

* 웹 서버 앞단에서 요청 필터링
* DDoS 공격 방어
* HTTPS 구성 지원

### 3️⃣ 트래픽 보호

* 해외에서 발생하는 의심스러운 트래픽 감지
* CAPTCHA 기반으로 봇/공격 차단
* 불필요한 클라우드 비용 증가 방지

---

## 11. 한 흐름으로 요약

* JavaScript Proxy → 객체 동작 가로채기
* Proxy 기반 옵저버 패턴 → 상태 변경 감지
* Vue 3 → Proxy 기반 반응성 시스템
* 프록시 패턴 → 접근 제어용 디자인 패턴
* 프록시 서버 → 네트워크 레벨 중계
* Cloudflare → 프록시 서버 기반 CDN + 보안

---

## 12. 한 줄 정리

> **프록시는 "대상 앞단에서 흐름을 가로채는 개념"이며,
> JavaScript, 프레임워크, 서버, CDN까지 동일한 철학으로 확장된다.**

---

## 📌 키워드

* Observer Pattern
* JavaScript Proxy
* Proxy Pattern
* Reactive System
* Vue.js 3
* Proxy Server
* CDN
* Cloudflare
