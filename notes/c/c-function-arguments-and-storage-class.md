📌 C Language: Function Arguments & Storage Classes 정리
1️⃣ 포인터에 의한 전달 (Call by Pointer)

함수 안에서 인자의 값을 변경해야 하는 경우에는
값에 의한 전달(Call by Value)이 아니라 포인터에 의한 전달을 사용해야 한다.

🔹 사용 절차

함수 선언

처리 결과를 받아올 매개변수를 포인터형으로 선언한다.

함수 호출

처리 결과를 받을 변수의 주소(&) 를 인자로 전달한다.

함수 정의

매개변수가 가리키는 곳에 처리 결과를 저장한다.

void changeValue(int *x) {
    *x = 10;
}

int main() {
    int a = 5;
    changeValue(&a);
}

2️⃣ 배열을 인자로 갖는 함수
🔹 배열 매개변수 선언

아래 두 선언은 완전히 같은 의미이다.

int *arr
int arr[]

🔹 배열 전달 시 규칙
1. 함수 선언

매개변수는 포인터형 또는 크기를 생략한 배열로 선언

함수 안에서 배열이 변경되지 않을 경우 → const 사용

배열의 크기가 필요하면 크기도 함께 매개변수로 전달

void printArray(const int *arr, int size);

2. 함수 호출

배열의 시작 주소(배열 이름) 를 인자로 전달

필요 시 배열 크기도 함께 전달

int arr[5] = {1,2,3,4,5};
printArray(arr, 5);

3. 함수 정의

매개변수인 포인터를 배열처럼 사용하면 된다.

void printArray(const int *arr, int size) {
    for(int i = 0; i < size; i++)
        printf("%d ", arr[i]);
}

3️⃣ 구조체를 인자로 전달하는 경우
🔹 전달 방식

구조체는 값으로 전달할 수도 있지만

포인터로 전달하는 것이 효율적이다 (복사 비용 ↓)

🔹 사용 규칙
1. 함수 선언

구조체가 변경되지 않으면 const 구조체 포인터 사용

void printStudent(const struct Student *s);

2. 함수 호출

구조체 변수의 주소를 전달

printStudent(&st);

3. 함수 정의

포인터를 이용해 구조체 멤버 접근 (->)

void printStudent(const struct Student *s) {
    printf("%d %s\n", s->id, s->name);
}

4️⃣ 기억 부류 (Storage Class)
🔹 auto 변수

지역 변수의 기본 기억 부류

함수 블록 내부에서 선언

블록 종료 시 메모리 해제

void func() {
    auto int x = 10; // 생략 가능
}

🔹 register 변수

변수를 메모리 대신 CPU 레지스터에 저장 시도

메모리 접근보다 빠름

레지스터 부족 시 할당되지 않을 수도 있음

❗ 주소 연산(&) 불가능 → 사용 시 컴파일 에러

register int i;

🔹 extern 변수

해당 변수가 다른 파일에 선언되어 있음을 컴파일러에 알림

실제 메모리를 할당하지 않음

전역 변수 전방 선언에 사용

extern int globalVar;

🔹 static 변수
✅ 정적 지역 변수

함수 내부에서 선언

프로그램 시작 시 메모리 할당

함수 종료 후에도 값 유지

선언된 함수 내부에서만 접근 가능

void counter() {
    static int count = 0;
    count++;
}

✅ 정적 전역 변수

전역 변수에 static 지정

해당 소스 파일에서만 접근 가능

다른 파일 접근 차단 (캡슐화 효과)

static int onlyThisFile;

✅ 요약 한 줄 정리

값 변경 필요 → 포인터 전달

배열 전달 → 주소 + 필요 시 크기

구조체 전달 → 포인터 권장

static → 수명 연장

extern → 다른 파일 전역 변수 참조

register → 빠르지만 주소 불가