# C 언어 문자열·배열·포인터 핵심 정리

> GitHub 업로드용 요약 노트 (기초 + 실무에서 자주 실수하는 포인트 위주)

---

## 1. 문자 배열과 문자열

### 1️⃣ 널 문자(\0)의 중요성

* C에서 문자열은 **문자 배열(char[])** 로 표현된다.
* 문자열의 끝에는 반드시 **널 문자 '\0'** 가 저장되어야 한다.
* 따라서 문자 배열 선언 시:

```c
char str[6] = "Hello"; // 5글자 + '\0'
```

➡️ 배열 크기 = 문자열 길이 + 1

---

### 2️⃣ 문자 배열에 문자열 대입 ❌

* 문자 배열은 **배열 전체에 문자열을 직접 대입할 수 없다.**

```c
char a[10];
a = "Hello";   // ❌ 컴파일 에러
```

* 문자열 변경 시에는 아래 함수 사용:

```c
strcpy(a, "Hello");
```

---

### 3️⃣ 문자열 연결: `strcat()` 사용 시 주의

* `strcat(dest, src)`는 **dest 뒤에 src를 붙인다**
* dest 배열의 크기가 충분하지 않으면 **버퍼 오버플로우 발생**

```c
char a[20] = "Hi ";
char b[] = "C";
strcat(a, b); // OK
```

⚠️ 항상 **배열 크기 확인 필수**

---

### 4️⃣ 문자열 입력 함수 주의사항

#### `scanf("%s", str)`

* 공백(스페이스) 전까지만 입력받음

#### ❌ `gets()` 사용 금지

* **보안 취약점** 때문에 C11 표준에서 제거됨

#### ✅ 대체 함수

```c
fgets(str, sizeof(str), stdin);
```

---

### 5️⃣ 문자열 출력 및 포맷 변경

#### `sprintf()`

* 문자열을 특정 형식으로 가공해 저장

```c
sprintf(msg, "%s 씨 안녕하세요?", name);
```

#### `puts()`

* 문자열 한 줄 출력 (자동 개행)

```c
puts(msg);
```

---

## 2. 포인터(pointer)

### 1️⃣ 포인터 기본 개념

* 포인터는 **주소를 저장하는 변수**

```c
int a = 10;
int *p = &a;
```

---

### 2️⃣ 포인터 크기

* 포인터 변수의 크기는 **가리키는 타입과 무관**
* 시스템에 따라 다름

  * 32bit: 4바이트
  * 64bit: 8바이트

```c
sizeof(int*) == sizeof(char*)
```

---

### 3️⃣ 포인터 타입 일치 중요

* 포인터의 타입은 **가리키는 변수 타입과 반드시 일치**해야 한다

```c
int a;
char *p = &a; // ❌ 타입 불일치
```

---

### 4️⃣ 배열과 포인터의 관계

```c
arr[i]      == *(arr + i)
&arr[i]     == arr + i
```

* 배열 이름은 **배열의 시작 주소**를 의미

---

### 5️⃣ 배열과 포인터의 차이점

| 구분    | 배열   | 포인터  |
| ----- | ---- | ---- |
| 주소 변경 | ❌ 불가 | ✅ 가능 |
| 메모리   | 고정   | 가변   |

```c
int arr[3];
int *p;
p = arr;   // 가능
arr = p;   // ❌ 불가능
```

---

## 3. const와 포인터 (시험·실무 핵심)

```c
const int *p;     // 값 변경 ❌, 주소 변경 ⭕
int *const p;     // 값 변경 ⭕, 주소 변경 ❌
const int *const p; // 값 ❌ 주소 ❌
```

| 선언           | 의미              |
| ------------ | --------------- |
| const int *p | p가 가리키는 값 변경 불가 |
| int *const p | p 자체(주소) 변경 불가  |

---

## 4. 추가로 알아두면 좋은 포인트 ⭐

### ✅ 문자열 리터럴 주의

```c
char *p = "Hello"; // 읽기 전용 메모리
p[0] = 'h';         // ❌ 런타임 에러 가능
```

➡️ 수정 필요하면 반드시 배열 사용

```c
char p[] = "Hello";
```

---

### ✅ sizeof vs strlen 차이

```c
char a[10] = "Hi";

sizeof(a);  // 10
strlen(a);  // 2
```

---

### ✅ NULL 포인터 체크 습관

```c
int *p = NULL;
if (p != NULL) {
    *p = 10;
}
```

---

### ✅ 함수 인자로 배열 전달 = 포인터

```c
void func(char arr[]) {
    // 실제로는 char *arr
}
```

---

## 📌 한 줄 요약

* 문자열 = char 배열 + '\0'
* 배열은 주소 변경 ❌, 포인터는 ⭕
* 문자열 함수는 **버퍼 크기 항상 체크**
* `gets()` ❌ → `fgets()` ⭕
* const 포인터 조합은 **시험 단골**

---


